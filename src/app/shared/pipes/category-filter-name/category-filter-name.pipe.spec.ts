import { CategoryFilterNamePipe } from './category-filter-name.pipe';

describe('CategoryFilterNamePipe', () => {
  it('create an instance', () => {
    const pipe = new CategoryFilterNamePipe();
    expect(pipe).toBeTruthy();
  });
});
